describe("WMasterChef", function () {
  before("setup accounts & deploy libraries", async () => {
    // something
  });

  beforeEach("deploy all contracts", async () => {
    // something
  });
});
